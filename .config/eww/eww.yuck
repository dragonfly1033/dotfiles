

;class
;valign - fill baseline center start end
;halign - fill baseline center start end
;vexpand - bool
;hexpand - bool
;width
;height
;active
;tooltip
;visibility

; (include "./sideBar.yuck")
(include "./trys/sideBar.yuck")

(defwindow	main
	:monitor 0
	:geometry (geometry	:x "0%"
						:y "0%"
						:width "24px"
						:height "100%"
						:anchor "bottom left")
	:stacking "fg"
	;:reserve (struts :distance "24px" :side "left")
	:style "background-color: #00ffff"
	(inner)
)




