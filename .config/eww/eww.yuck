(include "./modules/toggle.yuck")

(defwindow	settings
	:monitor 0
	:geometry (geometry	:x "0%"
						:y "0%"
						:width "30%"
						:height "30%"
						:anchor "bottom left")
	:stacking "fg"
	; :reserve (struts :distance "24px" :side "left")
	
	(toggle :class "toggle"
			:texton "on" 
			:textoff "off" 
			:funcon "notify-send on"
			:funcoff "notify-send off"
	)
)

