#!/bin/sh

name=$1
if [ -z $name ]; then
	$EDITOR $HOME/Documents/notes/temp-$(date +"%y-%m-%d--%H:%M:%S")
elif [ $name == "list" ]; then
	micro "$HOME/Documents/notes/$(ls $HOME/Documents/notes | fzf)"
else
	$EDITOR $HOME/Documents/notes/$name
fi
