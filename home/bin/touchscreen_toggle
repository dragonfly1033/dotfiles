#!/bin/sh

state=$(cat ~/bin/.env/TOUCHSCREEN_STATE)

if [ $state == "ON" ]; then
	xinput disable 14
	echo "OFF" > ~/bin/.env/TOUCHSCREEN_STATE
else
	xinput enable 14
	echo "ON" > ~/bin/.env/TOUCHSCREEN_STATE
fi
