#!/bin/sh

theme="$1"

if [ -n "$theme" ]; then
    sed 's/include.*/include'" $theme/" -i ~/.config/zathura/zathurarc
else
    themes=$(ls -1 ~/.config/zathura | sed '/zathurarc/d;/change_theme/d')

    notify-send "Valid Themes:" "$themes"
fi
