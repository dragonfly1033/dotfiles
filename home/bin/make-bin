#!/bin/sh

name=$1

touch $HOME/bin/$name
echo "#!/bin/sh" >> $HOME/bin/$name
echo "" >> $HOME/bin/$name
echo "" >> $HOME/bin/$name
echo "" >> $HOME/bin/$name
chmod +x $HOME/bin/$name
$EDITOR $HOME/bin/$name 
