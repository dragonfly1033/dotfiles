#!/bin/sh

ddmenu_dir=~/bin/dmenu_dir

lang=$(dmenu "Python\nGo\nHTML" "Lang: ")

CONF=$($ddmenu_dir ~/Documents/"$lang")

if [ $CONF != 'NONE' ]; then 
	#notify-send "$CONF" 
	eval "vscodium $CONF" 
fi	

