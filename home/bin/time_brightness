#!/bin/sh

hour=$(date +"%H")
brightness=$HOME/bin/brightness

if [ $hour -ge 23 ] || [ $hour -le 6 ]; then
	$brightness set 0
	$HOME/bin/set_redshift 0
else
	$brightness set 75
	$HOME/bin/set_redshift 10
fi
