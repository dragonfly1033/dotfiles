#!/bin/sh

hour=$(date +"%H")
brightness=$HOME/bin/brightness

killall redshift
redshift -x

if [ $hour -ge 23 ] || [ $hour -le 6 ]; then
	$brightness set 0
	(redshift &) && disown
else
	$brightness set 75
	killall redshift
	redshift -x
fi
