#!/bin/sh

POLYBAR_BOTTOM=$(cat $HOME/bin/.env/POLYBAR_BOTTOM)
if [ $POLYBAR_BOTTOM == "true" ]; then
	echo "false" > $HOME/scipts/.env/POLYBAR_BOTTOM
elif [ $POLYBAR_BOTTOM == "false" ]; then
	echo "true" > $HOME/bin/.env/POLYBAR_BOTTOM
fi
polybar-msg cmd restart
